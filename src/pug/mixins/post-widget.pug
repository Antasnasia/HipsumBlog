mixin post-widget(title, info, img, link="#")
    div.post-widget 
        .post-widget-img 
            a(href= link)
                img(src="./img/small-thumbs/" + img, alt= title)
        h4.post-widget-title 
            a(href=link) #{title}
        .post-widget-info #{info} 